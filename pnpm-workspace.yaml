packages:
  - packages/*

allowBuilds:
  esbuild: true

# The plugins consume each engine release the moment it is published, so the
# engine packages are exempt from pnpm's minimum-release-age policy.
minimumReleaseAgeExclude:
  - '@nilvn/*'
