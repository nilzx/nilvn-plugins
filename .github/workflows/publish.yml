# Publish @nilvn/plugins to npm when a plugins-v* tag is pushed, through npm
# trusted publishing (no secret needed).
# The tag must equal the package version (checked below), which
# `pnpm version:set plugins x.y.z` maintains.
name: Publish

on:
  push:
    tags: ['plugins-v*']

# contents:read to check out; id-token:write so pnpm publishes through npm's
# trusted publishing (OIDC): each package lists this repository + workflow as a
# trusted publisher on npmjs.com, so no npm token and no one-time code is
# involved (token publishing is disabled on the packages).
permissions:
  contents: read
  id-token: write

jobs:
  publish:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7

      - uses: pnpm/action-setup@v6
        with:
          version: 11

      - uses: actions/setup-node@v7
        with:
          node-version: 22
          cache: pnpm
          registry-url: https://registry.npmjs.org

      - run: pnpm install --frozen-lockfile

      - name: Tag must match the package version
        run: |
          v="$(node -p "require('./packages/plugins/package.json').version")"
          test "plugins-v$v" = "$GITHUB_REF_NAME" || { echo "tag $GITHUB_REF_NAME != package version $v"; exit 1; }

      - run: pnpm typecheck
      - run: pnpm test
      - run: pnpm build
      - run: pnpm pack:smoke

      # `prepack` rebuilds the package; --no-git-checks because the tag ref is
      # detached. A version already on npm (published by hand) is skipped.
      - name: Publish
        run: |
          v="$(node -p "require('./packages/plugins/package.json').version")"
          if npm view "@nilvn/plugins@$v" version >/dev/null 2>&1; then
            echo "@nilvn/plugins@$v is already on npm — skipping"
          else
            pnpm --filter @nilvn/plugins publish --access public --no-git-checks
          fi
