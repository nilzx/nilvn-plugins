---
name: Bug report
about: A first-party plugin misbehaves
labels: bug
---

**Versions**
`@nilvn/plugins` x.y.z with `@nilvn/engine` x.y.z

**Plugin and command**
Which plugin, and the script line(s) that trigger it.

**What happened / what you expected**

**Diagnostics**
The entries in `engine.diagnostics` (or the `onError` output), if any.

**Environment**
Browser and version; bundler, `<script>` tag or the demo.
